\documentclass{article}

% 说明：这里是隔离测试入口。
% 后续当 Hypo-Note/Hypo-Sheet 落地后，可以把这里改成：
% \usepackage{../sty/classes/note/Hypo-Note}

% v0.2.0 Step A：只测字体层（避免引入 Box/Code/Refs 等变量）
\usepackage{../sty/core/Hypo-Fonts}

% v0.2.1 Step B：只测颜色层（验证色名可用）
\usepackage{../sty/core/Hypo-Colors}

% v0.2.2 Step C：只测 Base 参数导出（FinalOutputDir）
\usepackage[outputdir=build]{../sty/core/Hypo-Base}

% v0.2.3 Step D：数学与速写（默认开启；可通过 \HypoDisableShorthand 关闭）
\usepackage{../sty/core/Hypo-Math}

\begin{document}
\section*{Fonts Test}

English: The quick brown fox jumps over the lazy dog.

中文：这是一个字体测试用例。用于验证 XeLaTeX + xeCJK 能正常工作。

\section*{Colors Test}
{\color{HypoOrange}HypoOrange}\quad
{\color{HypoCyan}HypoCyan}\quad
{\color{HypoRed}HypoRed}\quad
{\color{HypoBlue}HypoBlue}\quad
{\color{HypoDarkBlue}HypoDarkBlue}

\section*{Base Test}
FinalOutputDir = \texttt{\FinalOutputDir}

\section*{Math + Shorthand Test (ON by default)}
\TBF{Bold text} / \TX{inline text}

Math: $\MB{x} + \MC{F}(\BS{\theta})$ and $\Abs{-3} = 3$.
\end{document}
