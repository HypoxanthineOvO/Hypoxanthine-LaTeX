\documentclass{article}

% 主线：通过入口包验证“切入口尽量只改引入文件”
% v0.2.4 Step E：入口骨架（Hypo-Note / Hypo-Sheet）
\usepackage[outputdir=build]{Hypo-Note}

\begin{document}
\section*{Fonts Test}

English: The quick brown fox jumps over the lazy dog.

中文：这是一个字体测试用例。用于验证 XeLaTeX + xeCJK 能正常工作。

\section*{Colors Test}
{\color{HypoOrange}HypoOrange}\quad
{\color{HypoCyan}HypoCyan}\quad
{\color{HypoRed}HypoRed}\quad
{\color{HypoBlue}HypoBlue}\quad
{\color{HypoDarkBlue}HypoDarkBlue}

\section*{Base Test}
FinalOutputDir = \texttt{\FinalOutputDir}

\section*{Math + Shorthand Test (ON by default)}
\TBF{Bold text} / \TX{inline text}

Math: $\MB{x} + \MC{F}(\BS{\theta})$ and $\Abs{-3} = 3$.

\section*{Box Test (Definition)}
% 1. 定义一个 Definition，显式指定 label=group（最终 label 为 "def:group"）
\begin{definition}{Group Definition}{group}
  A group is a set...
\end{definition}

See \cref{def:group}.

% 2. 定义一个 Example，显式指定 label=integers（最终 label 为 "ex:integers"）
\begin{example}{Integers}{integers}
  $\mathbb{Z}$ is a group...
\end{example}


% 3. 如果不需要引用，标签参数留空即可（不会产生 \label）
\begin{note}{Look at this}{mynote}
  Just a note.
\end{note}

\cref{ex:integers}
\cref{note:mynote}

\section*{Img Test}
% \img will automatically prefix labels with "fig:".
\img[label=demo]{assets/example-image.pdf}{A tiny embedded example image.}

See \cref{fig:demo}.

\section*{Algorithm Test}
\begin{algorithm}[htbp]
  \caption{Demo algorithm}
  \label{alg:demo}
  \KwIn{Input $x$}
  \KwOut{Output $y$}
  \BlankLine
  $y \leftarrow x$
\end{algorithm}

See \cref{alg:demo}.
\end{document}
