# ==========================================
# Hypoxanthine Tests (Isolation Sandbox)
# ==========================================

# 目标：给 corner cases 一个独立入口，不影响 manual/

NAME = Test
MAIN_FILE = Test.tex
HYPO_PATH = ..

include $(HYPO_PATH)/make/Hypoxanthine.mk
