#+#+#+#+----------------------------------
# Hypoxanthine Manual Build
#+#+#+#+----------------------------------

# 1) 输出 PDF 名称（建议与主文件同名，避免 make 核心输出提示与实际产物不一致）
NAME = Manual

# 2) 主文件入口
MAIN_FILE = Manual.tex

# 3) 指向仓库根目录（manual/ 的上一级）
HYPO_PATH = ..

# 4) 引入构建核心（注意 Linux 大小写敏感）
include $(HYPO_PATH)/make/Hypoxanthine.mk

