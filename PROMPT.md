# Hypoxanthine-LaTeX 协作 Prompt（粘贴即用）

把你的需求粘贴到下方模板里发给我即可。

要求（默认）：
- 先更新 [DEVELOPMENT_PLAN.md](DEVELOPMENT_PLAN.md) 与 [FEATURES.md](FEATURES.md)
- 版本发布时同步更新 [CHANGELOG.md](CHANGELOG.md) 并打 git tag
- 未经你明确同意，不改 `.sty` 代码、不新增环境/盒子
- 改代码时优先保证：Linux 大小写一致、构建可跑、示例可编译

---

## 1) 本次要改什么（用一句话概括）


## 2) 影响范围
- 入口：Hypo-Note / Hypo-Sheet（是否都要一致？）
- 速写：是否开启大写命令（例如 \T / \TBF）
- 盒子：definition / example / note（是否新增或修改？）
- label：是否需要中文拼音？还是 hash 即可？
- 缩进：indent=true/false 默认值？

## 3) 具体需求（越具体越好）
- 新增/修改的命令：
- 新增/修改的环境：
- 配色/样式变更：
- 行为开关：

## 4) 不要做什么（明确禁止项）


## 5) 验收方式
- 我希望看到：
- 最小示例：

## 6) 版本与发布（如果本次需要 bump version）
- 目标版本号：例如 v0.2.0
- CHANGELOG：本次新增/变更/修复点（写入 [CHANGELOG.md](CHANGELOG.md)）
- Tag：是否需要我执行 `git tag -a <version> -m "<version>"`（默认需要）

---

（在此粘贴你的需求）
