\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{Hypo-Code}[2026/01/01 Hypoxanthine Code Plugin]

\RequirePackage{../../sty/core/hypo-base} % 获取 \FinalOutputDir

\RequirePackage[outputdir=\FinalOutputDir]{minted}

\setminted{
    frame=lines,
    framesep=2mm,
    baselinestretch=1.1,
    fontsize=\small,
    linenos,
    breaklines
}